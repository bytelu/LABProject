<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.LABProject" parent="Theme.MaterialComponents.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <!--<item name="colorPrimary">@color/white</item> -->
        <item name="colorPrimaryDark">@color/ipnNivel2</item>
        <item name="colorAccent">@color/ipnNivel3</item>
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
        <item name="android:windowContentTransitions">true</item>
        <item name="colorPrimary">@color/white</item>
        <item name="android:textColorPrimary">@color/white</item>
        <item name="android:statusBarColor">@color/color_launch</item>
    </style>

    <style name="Theme.LABProject" parent="Base.Theme.LABProject" />


    <style name="ThemeOverlay.AppCompat.navTheme">
        <item name="colorPrimary">@color/ipn</item>
    </style>
</resources>