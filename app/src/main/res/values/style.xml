<?xml version="1.0" encoding="utf-8"?>
<resources>
    <style name="SearchViewStyle" parent="Widget.AppCompat.SearchView">
        <item name="android:textColor">@android:color/black</item>
        <item name="android:textColorHint">@android:color/darker_gray</item> <!-- Color del hint -->
    </style>
    <style name="AlertDialogStyle" parent="Theme.AppCompat.Light.Dialog.Alert">
        <item name="android:textColorPrimary">@color/white</item> <!-- Color del texto principal -->
        <item name="android:background">@color/ipnNivel2</item> <!-- Fondo del cuadro de diálogo -->
    </style>
</resources>